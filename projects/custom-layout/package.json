{
  "name": "custom-layout",
  "version": "0.0.1",
  "scripts": {
    "build": "ng build --project=custom-layout",
    "pack": "npm pack ../libs/custom-layout",
    "rebuild": "npm run build && npm run pack && cd ../../ && npm i ./projects/custom-layout  "
  },
  "peerDependencies": {
    "@angular/common": "^10.0.14",
    "@angular/core": "^10.0.14"
  },
  "dependencies": {
    "tslib": "^2.0.0"
  }
}
